#include <stdio.h>   // 1. Header file

int main() {         // 2. Main function starts

    int a, b;        // 3. Variable declaration

    a = 10;          // 4. Program body (logic)
    b = 20;

    printf("Sum = %d", a + b);

    return 0;        // 5. Program ends
}