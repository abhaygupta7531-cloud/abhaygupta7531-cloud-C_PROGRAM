#include <stdio.h>

int main() {
    int a = 5, b = 10;   // Declare and initialize variables

    // Checks whether a is equal to b
    // Returns 1 if true, otherwise 0
    printf("The value A is: %d\n", a == b);

    // Checks whether a is NOT equal to b
    printf("The value B is: %d\n", a != b);

    // Checks whether a is less than b
    printf("The value H is: %d\n", a < b);

    // Checks whether a is greater than b
    printf("The value A is: %d\n", a > b);

    // Checks whether a is less than or equal to b
    printf("The value Y is: %d\n", a <= b);

    return 0;   // End of program

   // printf("The value of a is %d and the value of b is %d and sum of both %d\n",a,b,c);
    //printf("The remainder whan a is divided by b is:%d\n",a%b);
    // Modulus operator is used to get the remainder
     
}